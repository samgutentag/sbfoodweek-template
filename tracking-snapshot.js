// Baked tracking snapshot for a concluded event.
// Left empty during the event — stats pages fall back to the live Worker.
// scripts/wind-down.py replaces this with `const TRACKING_SNAPSHOT = {...}`
// so the stats dashboard keeps working after tracking is disabled.
